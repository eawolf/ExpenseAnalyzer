spring:
  application:
    name: notification-service
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: notification-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"
        spring.json.type.mapping: "com.expenseanalyzer.auth.dto.NotificationEvent:com.expenseanalyzer.notification.model.NotificationEvent"
  mail:
    host: smtp.gmail.com
    port: 587
    username: codersatyam666@gmail.com
    password: ddwomyahcjovbknp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
server:
  port: 8083

logging:
  file:
    name: ${LOG_DIR:logs}/${spring.application.name}.log
